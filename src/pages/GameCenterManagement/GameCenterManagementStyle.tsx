import styled from "@/styles/styled-components";
import { theme } from "@/styles/theme";

export const GameCenterManagementStyle = styled.div`
    h3, p {
        margin: 0.5rem;
    }

    button {
        margin-left: 10px;
        float:right;
    }
` as any;